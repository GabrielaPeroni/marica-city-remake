# Copy this file to .env and fill in real values for your environment.
# Django will read these via python-decouple's config() calls in config/settings/*.py.

# Which settings module to use (default: config.settings.dev if unset).
# Local dev/tests don't need this at all. Production deployments should set
# it explicitly: DJANGO_SETTINGS_MODULE=config.settings.prod
# DJANGO_SETTINGS_MODULE=config.settings.dev

# Django secret key used for cryptographic signing (required, no default — generate a unique value)
SECRET_KEY=change-me-to-a-random-secret-key

# Enable Django debug mode (default: False)
DEBUG=True

# Comma-separated list of allowed hostnames (default: localhost,127.0.0.1)
ALLOWED_HOSTS=localhost,127.0.0.1

# Database backend engine (default: django.db.backends.sqlite3)
DB_ENGINE=django.db.backends.sqlite3

# Database name / path (default: BASE_DIR/db.sqlite3 for sqlite3)
DB_NAME=db.sqlite3

# Database user (default: empty, unused for sqlite3)
DB_USER=

# Database password (default: empty, unused for sqlite3)
DB_PASSWORD=

# Database host (default: empty, unused for sqlite3)
DB_HOST=

# Database port (default: empty, unused for sqlite3)
DB_PORT=

# Google OAuth client ID used for "Sign in with Google" (required, no default)
GOOGLE_OAUTH_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com

# Google Maps API key, used for map embeds (default: empty/optional)
GOOGLE_MAPS_API_KEY=

# Enable django-ratelimit protections (default: True)
RATELIMIT_ENABLE=True

# Cache backend (also backs django-ratelimit). Leave empty to use the
# per-process LocMemCache, which is fine for local dev but NOT safe for a
# multi-process/multi-worker production deployment. Set one of these to a
# Redis connection URL to switch to django-redis instead (default: empty):
# CACHE_URL=redis://localhost:6379/0
CACHE_URL=
# REDIS_URL is checked as a fallback if CACHE_URL is unset (useful with
# PaaS providers that inject REDIS_URL automatically).
REDIS_URL=

# --- Production-only settings (config.settings.prod) -----------------------
# These are only read when DJANGO_SETTINGS_MODULE=config.settings.prod.
# Each defaults to the secure value in prod, but can be relaxed for a
# staging environment that isn't served over HTTPS yet.

# Redirect all non-HTTPS requests to HTTPS (default: True in prod)
SECURE_SSL_REDIRECT=True

# Only send the session cookie over HTTPS (default: True in prod)
SESSION_COOKIE_SECURE=True

# Only send the CSRF cookie over HTTPS (default: True in prod)
CSRF_COOKIE_SECURE=True

# How many seconds to tell browsers to remember to use HTTPS (HSTS).
# Start conservative (default: 86400 = 1 day) and raise once HTTPS is
# confirmed stable, e.g. 31536000 for a full year.
SECURE_HSTS_SECONDS=86400

# Apply HSTS to all subdomains too (default: True in prod)
SECURE_HSTS_INCLUDE_SUBDOMAINS=True

# Allow this domain to be included in browsers' HSTS preload lists
# (default: True in prod) - only enable once you're sure, this is hard to
# reverse quickly.
SECURE_HSTS_PRELOAD=True

# Send X-Content-Type-Options: nosniff on all responses (default: True in prod)
SECURE_CONTENT_TYPE_NOSNIFF=True
